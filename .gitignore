# Build output
/ldap-kdc
/dist/

# Local state: the database and the key that decrypts it
*.db
*.db-wal
*.db-shm
master.key
*.keytab

# Local configuration and credentials
ldap-kdc.yaml
*.token
*.pem
*.crt
*.key

# The console's dependencies, and its build, which make build produces and is never committed
ui/node_modules/
internal/api/ui/dist/

# Editors
.idea/
.vscode/
